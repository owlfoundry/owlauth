# @owlauth/client

The official TypeScript client for [OwlAuth](https://github.com/owlfoundry/owlauth).

```typescript
import { Client } from "@owlauth/client";
```

This `0.0.1` release reserves the package name while the public API is being designed.
