name: Release Server

on:
  push:
    tags:
      - "server-v*"

permissions:
  contents: read

concurrency:
  group: release-server-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    name: Verify release tag
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      version: ${{ steps.release.outputs.version }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Prepare release manifests
        run: python3 scripts/release/prepare_release.py
      - name: Verify tag, version, and commit
        id: release
        run: |
          version="$(scripts/release/verify-release.sh server)"
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - name: Generate release changelog
        run: |
          python3 scripts/release/generate-changelog.py \
            --component server \
            --version "${{ steps.release.outputs.version }}" \
            --output release-notes.md
      - name: Upload release changelog
        uses: actions/upload-artifact@v7
        with:
          name: server-release-notes
          path: release-notes.md
          if-no-files-found: error
          retention-days: 7

  image:
    name: Build server image
    needs: verify
    uses: ./.github/workflows/publish-server-image.yml
    with:
      mode: release
      version: ${{ needs.verify.outputs.version }}
    permissions:
      contents: read
      packages: write

  build:
    name: Build server (${{ matrix.os }})
    needs: verify
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Prepare release manifests
        run: python3 scripts/release/prepare_release.py
      - name: Build server
        run: cargo build --release --locked --package owlauth-server
      - name: Package server
        env:
          VERSION: ${{ needs.verify.outputs.version }}
        run: |
          host="$(rustc -vV | sed -n 's/^host: //p')"
          binary="owlauth-server"
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            binary="owlauth-server.exe"
          fi
          archive="owlauth-server-${VERSION}-${host}.tar.gz"
          tar -czf "$archive" -C target/release "$binary"
      - name: Upload server artifact
        uses: actions/upload-artifact@v7
        with:
          name: owlauth-server-${{ matrix.os }}
          path: owlauth-server-*.tar.gz
          if-no-files-found: error
          retention-days: 7

  release:
    name: Create GitHub Release
    needs: [verify, build, image]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      packages: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Prepare release manifests
        run: python3 scripts/release/prepare_release.py
      - name: Download server artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: owlauth-server-*
          path: artifacts
          merge-multiple: true
      - name: Download release changelog
        uses: actions/download-artifact@v8
        with:
          name: server-release-notes
          path: release-metadata
      - name: Download verified server image
        uses: actions/download-artifact@v8
        with:
          name: server-container-image
          path: container-artifact
      - name: Generate exact-version plane OpenAPI artifacts
        run: |
          cargo run --release --locked --package owlauth-types --bin export-openapi -- \
            runtime artifacts/owlauth-runtime-openapi.json
          cargo run --release --locked --package owlauth-types --bin export-openapi -- \
            server artifacts/owlauth-server-openapi.json
          cargo run --release --locked --package owlauth-types --bin export-openapi -- \
            control artifacts/owlauth-control-openapi.json
      - name: Create checksums
        working-directory: artifacts
        run: |
          sha256sum \
            owlauth-server-*.tar.gz \
            owlauth-runtime-openapi.json \
            owlauth-server-openapi.json \
            owlauth-control-openapi.json > SHA256SUMS
      - name: Re-verify release eligibility
        run: scripts/release/verify-release.sh server >/dev/null
      - name: Require crates.io token
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: test -n "$CARGO_REGISTRY_TOKEN"
      - name: Publish server crates
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
          VERSION: ${{ needs.verify.outputs.version }}
        run: |
          scripts/release/publish-crate.sh \
            crates/owlauth-key-provider/Cargo.toml "$VERSION"
          scripts/release/publish-crate.sh \
            crates/owlauth-types/Cargo.toml "$VERSION"
          for attempt in {1..12}; do
            if scripts/release/publish-crate.sh \
              crates/owlauth-server/Cargo.toml "$VERSION"; then
              exit 0
            fi
            if [[ "$attempt" == 12 ]]; then
              exit 1
            fi
            sleep 10
          done
      - name: Load verified server image
        env:
          VERSION: ${{ needs.verify.outputs.version }}
        run: |
          gzip -dc container-artifact/server-image.tar.gz | docker load
          image="ghcr.io/${GITHUB_REPOSITORY,,}:${VERSION//+/_}"
          docker image inspect "$image" >/dev/null
      - name: Log in to GitHub Container Registry
        env:
          GHCR_TOKEN: ${{ github.token }}
        run: echo "$GHCR_TOKEN" | docker login ghcr.io --username "$GITHUB_ACTOR" --password-stdin
      - name: Publish versioned server image
        env:
          VERSION: ${{ needs.verify.outputs.version }}
        run: |
          image="ghcr.io/${GITHUB_REPOSITORY,,}"
          docker push "$image:${VERSION//+/_}"
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          VERSION: ${{ needs.verify.outputs.version }}
        run: |
          gh release create "server-v${VERSION}" artifacts/* \
            --notes-file release-metadata/release-notes.md \
            --title "OwlAuth Server v${VERSION}"
      - name: Promote server image to latest
        env:
          VERSION: ${{ needs.verify.outputs.version }}
        run: |
          image="ghcr.io/${GITHUB_REPOSITORY,,}"
          docker tag "$image:${VERSION//+/_}" "$image:latest"
          docker push "$image:latest"
