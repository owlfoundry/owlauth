import { Shell } from "../shared/Shell";

export function RuntimeApp() {
  return (
    <Shell eyebrow="OwlAuth Runtime" title="Hosted authentication">
      <p>No authentication interaction is active. Start sign-in from an OwlAuth Application.</p>
    </Shell>
  );
}
