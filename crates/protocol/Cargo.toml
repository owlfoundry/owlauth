[package]
name = "owlauth-protocol"
description = "Public protocol types for the OwlAuth server"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
utoipa = "5.5.0"

[dev-dependencies]
serde_json = "1.0.151"
